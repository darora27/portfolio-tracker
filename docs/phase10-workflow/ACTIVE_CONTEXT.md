<!-- GENERATED by scripts/phase10-generate-context.mjs. DO NOT EDIT. -->
# Phase 10 Active Context

This is the compact routing and safety packet for the next Phase 10 turn. It is
generated from the canonical workflow manifest, live state, and roadmap. Read
the deeper historical documents only when this packet or the current task names
them.

## Live route

- Phase: 10
- Current section: **§15 — Mission Control content rework**
- Managed range: §2–§18
- Stage: `review`
- Role: `claude_lead`
- Status: `ready`
- Next actor: `claude`
- Expected actor for this stage: `claude`
- Stop reason: none

The current state is before the
terminal section. Never infer the terminal section from prose; read
`workflow.managed_sections.terminal` from
`docs/phase10-workflow/workflow.json`.

## Required reading for this turn

Always read:

- `AGENTS.md`
- `PHASE10_STATE.json`
- `OWNER_FEEDBACK_LEDGER.md`
- `docs/phase10-workflow/ACTIVE_CONTEXT.md`

Then read the current, specifically routed sources:

- `docs/phase10-workflow/specs/section-15.md`

Historical sources are available on demand and are not recurring prompt
payload:

- `PHASE10_PROGRESS.md`
- `PRODUCT_DIRECTION.md`
- `docs/PHASE10_UX_ARCHITECTURE.md`
- `docs/PHASE10_AGENT_WORKFLOW.md`
- `PHASE10.md`
- `docs/phase10-workflow/IMPLEMENTATION_SPEC.md`

## Current section conditions

Roadmap title per the §13-acceptance split: 'Mission Control content rework', plus the Chart Room's stage two (the doors: a HOLDINGS row click, an ORBITS ring/blip click, FULL ANALYSIS ▸), which PHASE10.md's own text binds to this section. Authority: MISSION_CONTROL_ARCHITECTURE.md (owner-adopted whole, FB-34) for the content rework; §14's stage-two doors per PHASE10.md and the §14 design proof's freeze-boundary note. This is a landing section by the ledger's own rule 2 (18 open/designed rows counted at spec time, unchanged from §14's own count; this section closes eight of them by construction: FB-13 stage two, FB-27, FB-28, FB-29, FB-30, FB-32, FB-33, FB-35, plus executes FB-11's already-recorded retirement) -- PHASE10.md §15's own roadmap text and MISSION_CONTROL_ARCHITECTURE.md §10 are the authority for this scope; no further owner override needed because this section is itself the landing turn rule 2 requires. main was GREEN at this section's spec-time confirmation: npm test 118/118 files, 631/632 tests (1 intentional skip), npm run build exit 0, both independently re-run by the §14 accept turn at HEAD 739049d; not re-run this turn since no application source changed. Design proof: docs/phase10-workflow/design-proofs/section-15.md (existing-package-equivalent, citing MISSION_CONTROL_ARCHITECTURE.md). Spec and acceptance ledger written this turn -- see production_code_note_15. Highest-risk decision for the implementer: the three Chart Room doors (HOLDINGS row, ORBITS ring/blip, FULL ANALYSIS ▸) resolve to /stock/<ticker> in private mode ONLY; /stock/[ticker] is owner-gated and its own page.tsx header comment states it is 'never part of the public /share surface' -- public-mode doors must keep their exact pre-existing destinations unchanged (spec section 2). The ACTIVITY keep/cut verdict (architecture §9.2) is not due yet and is not a blocker. Turn hygiene note, 2026-07-31: a turn ended while its own background verification was still running, saying it would pick the work up when the sweep completed. Turns do not resume - the next turn is a cold process. Its work was left uncommitted and halted the relay on a dirty tree. A turn must either wait for its own verification or commit what it has with the result honestly recorded. Review round 3 wrote an independent sweep script and then ended while waiting for it, producing no results - the second consecutive turn lost the same way. The script is committed and is real work; nothing it was meant to measure was measured. The next review turn runs it in the FOREGROUND. A turn-hygiene rule has been added to both standing prompts.

Open bounded findings:

- {"id":"F1","criteria":["BHV-08","VIS-08"],"risk":"critical","summary":"The ORBITS ring click (as opposed to the blip dot) does not navigate to the correct real ticker in either mode.","root_cause":"SystemPlot.tsx's .radarRingTarget buttons are all absolutely centered on the identical pixel (orrery.module.css: position:absolute; top:50%; left:50%; transform:translate(-50%,-50%)) regardless of ring size, and border-radius:50% does not restrict a button's hit-test area to its visible stroke -- the full square bounding box is clickable. With no differentiating z-index in the default state, ordinary DOM-order stacking (not visual position) decides which ring receives a click near the shared center: the last-rendered (largest) ring wins for virtually the entire radar's clickable area.","reproduction":"docs/phase10-baseline/section-15/review/scripts/radar-blip-check.mjs and radar-hittest.mjs. Double-clicking ASML/GOOG/COST/MSFT/IBM's ring all navigated to /stock/CRM (5/5, deterministic, confirmed via document.elementsFromPoint at the radar's exact center). The blip affordance is unaffected and routes correctly for the same tickers.","scope_note":"SystemPlot.tsx/orrery.module.css's ring CSS is pre-existing and was not touched by this section's diff -- the defect is inherited, not newly introduced. It is in scope because §15 is the section that newly wires this exact click to the owner-gated /stock/<ticker> route and is the section that declares BHV-08/VIS-08, both explicitly requiring 'the correct real ticker'. PRV-01 is unaffected: the mis-routed click never produces a /stock/ URL in public mode.","evidence":["docs/phase10-baseline/section-15/review/raw-radar-ring-vs-blip-routing.json","docs/phase10-baseline/section-15/review/raw-radar-ring-hittest.json","docs/phase10-baseline/section-15/review/orbits-ring-overlap-1440.png"],"required_change":"Fix the ring's hit-test area so a click/double-click on a given ring resolves to that ring's own ticker, in both public and private mode. Mechanism (pointer-events scoping, clip-path, z-index ordering, or an equivalent) is the implementer's own judgment call -- not prescribed by this finding. Re-verify BHV-08 and VIS-08 with a script that clicks a NAMED ring/blip and asserts the resulting URL/destination contains that same ticker, for multiple tickers in both modes.","resolved":{"resolved_at":"2026-07-31","resolved_by":"codex_implementation (remediate), commit b27525be532fbac1c38f9559d9e436ad8b82f351","how":"The specific symptom described (every ring collapsing to the single last-DOM-rendered ring, CRM) is fixed by z-index-by-size stacking. A narrower variant of the same criteria (BHV-08/VIS-08) survives and is recorded as F2."}}
- {"id":"F2","criteria":["BHV-08","VIS-08"],"risk":"critical","summary":"Two of the largest ORBITS rings (CBRS, CRM) still navigate to a neighboring ring's ticker when clicked at a non-cardinal point on their own visible stroke; the smaller five rings are clean.","root_cause":"The F1 remediation (commit b27525b) stacks rings by z-index (holdings.length - index, smaller ring on top), which is geometrically correct for ring-vs-ring RECTANGLE containment: every ring's hit-box uses one ringSize% for both width and height, so all 8 boxes are self-similar (same aspect ratio, same center) and a smaller box is a strict geometric subset of every larger box in every direction -- there is no direction a smaller ring's rectangle can escape past a larger ring's own rectangle. The residual defect is a DIFFERENT mismatch: the visible stroke is an ellipse inscribed in that rectangle (border-radius:50% on the ::before), touching the rectangle's true edge only at the four cardinal points (0/90/180/270deg). At any other angle the visible curve sits inside the rectangle, short of its true edge -- and for the two outermost rings (CBRS, CRM), the next-smaller neighbor's rectangle (only ~12-14% smaller by ringSize%) still reaches out far enough in one axis-dominant direction to cover the point where the outer ring's visible curve is drawn, so the smaller (higher z-index) ring intercepts the click instead.","reproduction":"docs/phase10-baseline/section-15/review-2/scripts/radar-ring-sweep.mjs -- a 24-angle-per-ring hit-test sweep. The four smallest rings (ASML/GOOG/COST/MSFT's own neighbors) are clean at every sampled angle; MSFT/INTC/IBM misroute at 4/24 sampled angles (the four ~45deg diagonals); CBRS and CRM misroute at 12/24 -- half the compass. Confirmed with real dispatched double-clicks (docs/phase10-baseline/section-15/review-2/scripts/confirm-crm-45.mjs / raw-confirm-diagonal-misroute.json): CRM's own ring at 45deg opens /stock/IBM (private) / ?holding=IBM&camera=approach (public) instead of CRM's own destination, in both modes; CBRS's own ring at 45deg opens /stock/INTC / ?holding=INTC&camera=approach instead of CBRS's own destination.","scope_note":"Same BHV-08/VIS-08 criteria as F1 (both require the correct real ticker for the ring clicked); narrower in area (roughly half the compass on the two outermost rings, not virtually the whole radar) but identical in kind and risk. PRV-01 unaffected: the public-mode misroute still never produces a /stock/ URL, only a wrong-ticker ?holding= destination.","evidence":["docs/phase10-baseline/section-15/review-2/raw-radar-ring-sweep.json","docs/phase10-baseline/section-15/review-2/raw-confirm-diagonal-misroute.json","docs/phase10-baseline/section-15/review-2/raw-radar-ring-diagonal-click.json","docs/phase10-baseline/section-15/review-2/private-CRM-45deg-before.png","docs/phase10-baseline/section-15/review-2/public-CRM-45deg-before.png","docs/phase10-baseline/section-15/review-2/private-CBRS-45deg-before.png","docs/phase10-baseline/section-15/review-2/public-CBRS-45deg-before.png"],"required_change":"Fix the ring's hit-test area so it holds at every angle, not only the cardinal points the round-1 remediation's own test sampled. A mechanism that clips each ring's actual clickable area to its own visible annulus/ellipse (e.g. a clip-path sized to the ring's own stroke, keeping the ticker label as a sibling/absolutely-positioned element outside the clipped area so the label itself is unaffected) is one option; z-index-only stacking of same-shaped rectangles cannot fully close this because the defect is geometric (ellipse-vs-rectangle mismatch), not a stacking-order problem. Re-verify BHV-08 and VIS-08 with an angle SWEEP per ring (not a single fixed point), in both modes, plus a real double-click confirmation at any failing angles found.","resolved":{"resolved_at":"2026-07-31","resolved_by":"codex_implementation (remediate round 3), this turn's own commit -- left unreferenced by hash here since a commit cannot contain its own SHA; see this file's prev_actor_commit_note_18 for the preceding commit and production_code_note_18 for the full record. The reviewer turn should fill the commit hash from git log.","how":"Replaced the round-2 JS ellipse-containment-walk-with-margin heuristic (which the committed-but-failing stranded attempt, RING_HIT_MARGIN_PX=6, could not fully close -- see PHASE10_STATE.json's own F2 duplicate entry) with a geometric fix: clip-path: ellipse(50% 50% at 50% 50%) on each ring button restricts the browser's own native hit-test to that ring's TRUE visible disc, eliminating the rectangle-vs-ellipse mismatch at the source instead of patching around it with a magic-number margin. The ticker label moves to an unclipped sibling on >=1024px (clip-path would otherwise clip it away, since it renders past its ring's own edge); the <1024px fallback is untouched (clip-path is off there, label stays a button child exactly as before). Two independent full 24-angle x 8-ring x 2-mode sweeps (768 real dispatched double-clicks) plus an isolated-context retry pass over every flagged case (960 total): ZERO wrong-ticker misroutes. A small, non-reproducible click-delivery flakiness remained (different cases failing between runs, elementFromPoint confirming the correct ring was always under the pointer even on failing attempts) -- root-caused to Playwright's synthetic dblclick occasionally not registering in headless Chromium under sandbox load, not a hit-test defect; outside BHV-08/VIS-08's own correct-ticker criterion. See docs/phase10-workflow/acceptance/section-15.json BHV-08 for the full writeup."}}
- {"id":"F2","criterion":"radar ring click resolves to the wrong ticker","risk":"high","evidence":"docs/phase10-baseline/section-15/remediate-2/raw-radar-ring-angle-sweep-confirm.json","summary":"PREVIOUS ATTEMPT COMMITTED AND FAILING. A turn applied a 6px RING_HIT_MARGIN_PX ellipse-containment walk, started its confirmation sweep in the background, and ended before the sweep returned. The sweep FAILED. Owner view: 4 tickers still mis-hit (ASML 5 wrong angles, GOOG 3, MSFT 2, CBRS 1). Public view: 3 (ASML 3, GOOG 1, MSFT 2). This is NOT a passing candidate.","required_change":"Continue from the committed attempt rather than restarting - its diagnosis is sound and worth reading. The clickable box is a rectangle while the visible ring is an inscribed ellipse, so a smaller ring's rectangle corner can cover a point lying on a larger ring's curve; the fix walks outward from the ring that received the event to the first ring whose own ellipse contains the point. THE REMAINING FAILURE IS DIRECTIONAL, which is the lead: ASML's wrong hits cluster at 225, 240, 270 and 315 degrees - the lower-left quadrant - and all four resolve to /stock/GOOG. A uniform margin would not produce a quadrant-shaped failure, so suspect the containment test's handling of that quadrant (sign handling in the ellipse equation, or the walk terminating early) rather than simply raising RING_HIT_MARGIN_PX. Note also ASML deg 0 navigates to the focus URL rather than a stock page, which may be a separate defect.","attempted_and_insufficient":"RING_HIT_MARGIN_PX = 6","resolved":{"resolved_at":"2026-07-31","resolved_by":"codex_implementation (remediate round 3), this turn's own commit -- left unreferenced by hash here since a commit cannot contain its own SHA; see this file's prev_actor_commit_note_18 for the preceding commit and production_code_note_18 for the full record. The reviewer turn should fill the commit hash from git log.","how":"Same fix as the canonical F2 entry above (clip-path, not a bigger margin) -- this entry was this stranded attempt's own honestly-recorded failure evidence, retained for the record rather than a distinct defect needing separate closure."}}

Acceptance ledger:

- `docs/phase10-workflow/acceptance/section-15.json`

## Global gates

- **G-STOP:** STOP is checked before any other repository read or write.
- **G-SERIAL:** Exactly one coding agent may access the repository at a time; PHASE10_LOCK and the process check enforce this.
- **G-CLEAN:** The worktree is clean at turn start and fully committed at turn end.
- **G-BOUNDARY:** Security, privacy, authentication, financial correctness, and data integrity outrank product and visual choices.
- **G-VERIFY:** Implementation and remediation candidates require full tests and production build; final review repeats both independently.
- **G-EVIDENCE:** No visual, browser, mobile, accessibility, privacy, fallback, or performance check may be claimed without retained evidence.
- **G-SCOPE:** Review findings cite a declared acceptance criterion; remediation changes only bounded findings.
- **G-SECRETS:** Never read, print, edit, stage, or commit .env* contents, and never run vercel --prod.
- **G-PUBLIC:** New routes are owner-gated by default and new public data requires an explicit privacy decision and regression coverage.
- **G-DESIGN:** A user-facing section must satisfy the design-proof gate or cite an owner-approved equivalent before production implementation.

## State transitions

- `specify_pass` → `implement`
- `implement_pass` → `review`
- `review_fail` → `remediate`
- `remediate_pass` → `review`
- `review_pass` → `accept_and_advance`
- `accept` → `accept_and_advance`

A normal passing review accepts the verified candidate and initializes the next
section in the same Claude Lead turn. The legacy `accept` stage remains
supported only for already-routed state or explicit owner-exception handling.

## Verification contract

Two independent full gates remain mandatory: the implementation actor verifies the candidate before committing, and the lead independently verifies the final candidate during review. A passing review records acceptance in the same turn; no third full run exists only for bookkeeping.

- Implementation candidate: `npm test`, `npm run build`
- Independent final review: `npm test`, `npm run build`
- Documentation-only acceptance rerun: none

## Source hashes

These hashes make stale generated context mechanically detectable:

- `docs/phase10-workflow/workflow.json`: `d4d3d79d4cce68fee497e08cff2d9fdad3195046198d29afe20ad23e40d50050`
- `PHASE10_STATE.json`: `dc166d374227b22309f096e96d66f5be78e0c11d7cf824a6ca3f10902859eed8`
- `PHASE10.md`: `5100059a82f78bf77a51708fca43a127ddd5521e9d22d5b027ba23d9ccd0ee73`
- `docs/phase10-workflow/README.md`: `e433e1d9ce499eff3e2dcd6b1e9614ab04c0ddf8dc260a60a5566f4359a8c85d`
- `docs/PHASE10_AGENT_WORKFLOW.md`: `e9edfff440ec614bd1da26cc9e358987e6a4cd9953559e7391551c2f7a1a4804`
- `docs/phase10-workflow/DESIGN_GATE.md`: `bf5a35b998fc240e58f083da48093444cffd8daabfad450caa09256b255be25c`
- `docs/phase10-workflow/design-proofs/TEMPLATE.md`: `c50b4b9b94047b9686931b65f4d98133f8cb3a13ccead24fa8d3b9ec536f62f0`
- `docs/phase10-workflow/PROVIDER_OUTAGE.md`: `8488ee51a7e81dc24c4d17a1dc552a509bec40e0d1ebc9e241a9eb0ff6fdb5af`
- `docs/phase10-workflow/prompts/claude-lead.md`: `6248eff51e52682eff092c48088bade03ed087c6a99e34b4c85d0d8e2a22b5b0`
- `docs/phase10-workflow/prompts/codex-implementation.md`: `635916d0548b3b4de95755302c4774222c98ddf6a3fff91e4bc69283291905c8`
- `docs/phase10-handoffs/TEMPLATE.md`: `c4fec8f8169d5b3e18564c28823002c18dcc119cb0c1271ed1958e94fad705cf`
- `docs/phase10-workflow/acceptance/README.md`: `3bad4f6fb4e608fc9ba5f996bd3475dab2ee6a2d250c2a57b6f3c8c570e2ce2f`
