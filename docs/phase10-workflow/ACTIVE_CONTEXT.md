<!-- GENERATED by scripts/phase10-generate-context.mjs. DO NOT EDIT. -->
# Phase 10 Active Context

This is the compact routing and safety packet for the next Phase 10 turn. It is
generated from the canonical workflow manifest, live state, and roadmap. Read
the deeper historical documents only when this packet or the current task names
them.

## Live route

- Phase: 10
- Current section: **§10 — Universe colour, material, and command structure**
- Managed range: §2–§17
- Stage: `remediate`
- Role: `codex_implementation`
- Status: `blocked`
- Next actor: `devan`
- Expected actor for this stage: `codex`
- Stop reason: Round-3 remediation code and deterministic gates are complete, but every required unchanged live verifier is blocked before navigation: the in-app browser has no available session (discovery returned an empty list), and Chromium is denied MachPort bootstrap access by the CLI host (Permission denied 1100). The lead handoff makes these live results non-deferrable, so Devan must rerun or authorize the required live matrix in a browser-capable host before §10 can return to review.

The current state is before the
terminal section. Never infer the terminal section from prose; read
`workflow.managed_sections.terminal` from
`docs/phase10-workflow/workflow.json`.

## Required reading for this turn

Always read:

- `AGENTS.md`
- `PHASE10_STATE.json`
- `OWNER_FEEDBACK_LEDGER.md`
- `docs/phase10-workflow/ACTIVE_CONTEXT.md`

Then read the current, specifically routed sources:

- `docs/phase10-workflow/specs/section-10.md`
- `UNIVERSE_IDEAS_3.md`
- `UNIVERSE_PALETTE_3.html`
- `docs/reference/ (see its README.md - one mockup is superseded, and the planet mood reference must not be reproduced literally)`
- `docs/phase10-handoffs/2026-07-28-section-10-codex-implementation-to-devan-blocked.md`

Historical sources are available on demand and are not recurring prompt
payload:

- `PHASE10_PROGRESS.md`
- `PRODUCT_DIRECTION.md`
- `docs/PHASE10_UX_ARCHITECTURE.md`
- `docs/PHASE10_AGENT_WORKFLOW.md`
- `PHASE10.md`
- `docs/phase10-workflow/IMPLEMENTATION_SPEC.md`

## Current section conditions

Inserted by owner direction on 2026-07-28 (see roadmap_amendment_3). Authority order for this section: UNIVERSE_IDEAS_3.md (round-3 design, revision 2) -> UNIVERSE_PALETTE_3.html (the computed palette board) -> docs/reference/ -> PHASE10.md §10. Round 3 supersedes UNIVERSE_IDEAS_2.md wherever the two conflict. INHERITED RED, NOT A §10 DEFECT: npm test is red at this section start with exactly two failures in src/lib/observatory/planet-textures.test.ts - manifest.totalBytes 22,450,706 against §9’s 15,000,000 cap, and INTC 0.098092 / CBRS 0.093008 luminanceStdDev against §9’s 0.1 floor. Both were introduced by owner texture commits after §9’s reviewed implementation and are §10 work item 5 (texture regeneration): §10 must raise the byte ceiling in planet-textures.test.ts to the 30 MB figure PHASE10.md §10’s Build dimension already states, and must VERIFY - not assume - that relighting INTC and CBRS lifts them into the required luminance window. Ten owner defects reported live against §9 are carried into PHASE10.md §10 “Owner defects to close in this section” rather than reopening §9. Two owner observations remain carried unclosed from §8: D1 (unreproduced green-trail report - do not change colour logic until a contradicting ticker is named, then treat as severe) and D2 (“website is still relatively confusing”). §9’s testing prohibition carries forward and is restated in §10’s Acceptance: expect(source).toContain(...) is not valid coverage for rendered behaviour.

Open bounded findings:

- {"id":"F1","criterion":"TST-03","risk":"high","evidence":"docs/phase10-baseline/section-10/claude-review-3/raw-trail-sampler-TST-03.txt","summary":"sample-live-rgb.mjs still aborts (ASML deltaE 60.479 > 8). 7 of 8 holdings exceed deltaE 8 (ASML 60.72, GOOG 22.77, COST 12.29, MSFT 26.49, IBM 25.01, CBRS 33.90, NBIS 8.74); only INTC passes at 3.39. Hue lock fails on ASML (35.86deg off, chroma 0.165), MSFT (chroma 0.212) and IBM (14.93deg off). Two loss-direction ordering violations remain. Worse than round 2 (6 of 8), though the live payload moved between rounds so the two tables compare only in aggregate. Retained 9x9 pixel neighbourhoods and 120px crops show three separable mechanisms: the data-trail-sample point the app publishes for ASML sits inside the planet disc, so no trail pixel exists to sample there; the additive glow pass over the 0.09-width opaque core washes GOOG, MSFT, IBM and COST lighter than their model colour in a consistent direction; and the core is sub-pixel at the OVERVIEW camera on the outer orbits, so CBRS (a uniform #995047 across a one-pixel line) and NBIS blend with the void and read darker.","required_change":"Make rendered trail pixels match rampForWeekly() within deltaE 8 for every fixture and restore monotonic lightness ordering within each direction. Do not change the verifier, its thresholds, or its sample points. The misplaced ASML sample point must be understood first - it is the only holding whose failure the colour pipeline alone cannot explain."}
- {"id":"F2","criterion":"BLD-04","risk":"high","evidence":"docs/phase10-baseline/section-10/claude-review-3/raw-long-task-attribution.json","summary":"measure-long-tasks.mjs unmodified, five fresh 1440x900 CPU-2x contexts, not baseline-subtracted: 61/57/55/55/55 ms. Five of five breach the 50ms ceiling, unchanged within noise from round 2 (62/56/56/56/57). A CDP CPU profile on the same rig attributes the breaching task to Three.js WebGLRenderer shader-program acquisition, not texture upload: N at chunks/2tdjt19k1le0z.js:357 holds 34.3ms of self time inside the 68ms window, with getParameters and getProgramCacheKey on the same line, and about 50ms of self time across the 700-900ms bins. texSubImage2D never exceeds 2.5ms in any 100ms bin, so the round-2 staggered map binding worked and was not the cause.","required_change":"Bring the route-owned long task under 50ms across five fresh contexts, working from the shader-program attribution rather than a fresh guess. Do not baseline-subtract, redefine the gate, or add a post-processing pass. The material and program permutation count, and when programs are first compiled, are what the profile points at."}
- {"id":"F3","criterion":"DEF-02","risk":"high","evidence":"docs/phase10-baseline/section-10/claude-review-3/raw-strip-chirality-full-table.json","summary":"REGRESSION. capture-live-sphere-strip.mjs aborts with COST chirality failed: normal=-0.4561 mirrored=-0.4153. The full table fails 6 of 8 worlds (COST, MSFT, IBM, INTC, CBRS, NBIS) against 4 of 8 in round 2; CBRS (-0.1557 vs -0.0455) and NBIS (0.3887 vs 0.3901) moved from pass to fail. The decisive MSFT mirror signal is essentially unmoved (-0.4784/+0.2493 to -0.5005/+0.3117), so the alpha-mask compositor change did not alter the chirality of the mark that reaches the screen. IBM, INTC and NBIS fail inside noise, meaning those marks carry almost no measurable luminance signal in the sampled band - a second, separable problem. TST-04 passes, so the verifier is correct and this remains a texture-generation defect.","required_change":"Make normalScore > mirroredScore hold for all eight worlds as measured by the section verifier. MSFT is the unambiguous mirror case and the right starting point; the three near-zero worlds need the mark to produce a measurable signal before their result means anything. Do not weaken the assertion."}
- {"id":"F4","criterion":"VIS-12","risk":"high","evidence":"docs/phase10-baseline/section-10/claude-review-3/raw-question-duplicates.json","summary":"Two-thirds closed. capture-live-evidence.mjs ran end to end and reproduced all 16 after/ surfaces plus both mobile/ captures, and a live geometry probe measures 0px horizontal overlap between every CONTRIBUTION numeral and its signed bar across all 13 rendered rows - the defect that hid one character of every public financial value is fixed. Still open: round 2 required each active bay to render its question exactly once, and only MANIFEST was fixed. PLOT, MANIFEST, COMMS and LOG render once, but SCOPE, HAZARD and SIGNALS each render theirs twice when active - once in operationsBay > p.bayQuestion and once in the persistent railStations or instrumentStrip link.","required_change":"Suppress the persistent rail and instrument-strip question for SCOPE, HAZARD and SIGNALS while that bay is the active one, using the same active-panel rule already applied to PLOT and MANIFEST, then re-run capture-live-evidence.mjs. Nothing else in VIS-12 is outstanding."}

Acceptance ledger:

- `docs/phase10-workflow/acceptance/section-10.json`

## Global gates

- **G-STOP:** STOP is checked before any other repository read or write.
- **G-SERIAL:** Exactly one coding agent may access the repository at a time; PHASE10_LOCK and the process check enforce this.
- **G-CLEAN:** The worktree is clean at turn start and fully committed at turn end.
- **G-BOUNDARY:** Security, privacy, authentication, financial correctness, and data integrity outrank product and visual choices.
- **G-VERIFY:** Implementation and remediation candidates require full tests and production build; final review repeats both independently.
- **G-EVIDENCE:** No visual, browser, mobile, accessibility, privacy, fallback, or performance check may be claimed without retained evidence.
- **G-SCOPE:** Review findings cite a declared acceptance criterion; remediation changes only bounded findings.
- **G-SECRETS:** Never read, print, edit, stage, or commit .env* contents, and never run vercel --prod.
- **G-PUBLIC:** New routes are owner-gated by default and new public data requires an explicit privacy decision and regression coverage.
- **G-DESIGN:** A user-facing section must satisfy the design-proof gate or cite an owner-approved equivalent before production implementation.

## State transitions

- `specify_pass` → `implement`
- `implement_pass` → `review`
- `review_fail` → `remediate`
- `remediate_pass` → `review`
- `review_pass` → `accept_and_advance`
- `accept` → `accept_and_advance`

A normal passing review accepts the verified candidate and initializes the next
section in the same Claude Lead turn. The legacy `accept` stage remains
supported only for already-routed state or explicit owner-exception handling.

## Verification contract

Two independent full gates remain mandatory: the implementation actor verifies the candidate before committing, and the lead independently verifies the final candidate during review. A passing review records acceptance in the same turn; no third full run exists only for bookkeeping.

- Implementation candidate: `npm test`, `npm run build`
- Independent final review: `npm test`, `npm run build`
- Documentation-only acceptance rerun: none

## Source hashes

These hashes make stale generated context mechanically detectable:

- `docs/phase10-workflow/workflow.json`: `3d64a23fadd829c2c42d9722c2cd82225f1304bb0cb15017f4d925b257c8fa22`
- `PHASE10_STATE.json`: `4e5de1610abb90364ed4a40142fa59e331e5166af20b327215d6716fe6d15034`
- `PHASE10.md`: `c892047abfcaee0f146c1882b349b7cc1c267d354e22bae05712130a91e7d571`
- `docs/phase10-workflow/README.md`: `e433e1d9ce499eff3e2dcd6b1e9614ab04c0ddf8dc260a60a5566f4359a8c85d`
- `docs/PHASE10_AGENT_WORKFLOW.md`: `e9edfff440ec614bd1da26cc9e358987e6a4cd9953559e7391551c2f7a1a4804`
- `docs/phase10-workflow/DESIGN_GATE.md`: `bf5a35b998fc240e58f083da48093444cffd8daabfad450caa09256b255be25c`
- `docs/phase10-workflow/design-proofs/TEMPLATE.md`: `c50b4b9b94047b9686931b65f4d98133f8cb3a13ccead24fa8d3b9ec536f62f0`
- `docs/phase10-workflow/PROVIDER_OUTAGE.md`: `8488ee51a7e81dc24c4d17a1dc552a509bec40e0d1ebc9e241a9eb0ff6fdb5af`
- `docs/phase10-workflow/prompts/claude-lead.md`: `047eaf6097a5db80db6610ebea21d599a1c6fd73ed084352380bf77e166c5346`
- `docs/phase10-workflow/prompts/codex-implementation.md`: `da113815c122a9ee18ec6b8f5135fd9429f9e22cb5b21efb0f642ba21ee8f55b`
- `docs/phase10-handoffs/TEMPLATE.md`: `c4fec8f8169d5b3e18564c28823002c18dcc119cb0c1271ed1958e94fad705cf`
- `docs/phase10-workflow/acceptance/README.md`: `3bad4f6fb4e608fc9ba5f996bd3475dab2ee6a2d250c2a57b6f3c8c570e2ce2f`
