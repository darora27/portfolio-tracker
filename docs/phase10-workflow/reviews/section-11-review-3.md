# Phase 10 §11 review (turn 3) — FAIL with 2 bounded findings

Reviewed July 29–30, 2026 by `claude-code/sonnet-5` (Claude Lead, `stage: review`),
covering both roles under `single_provider_mode` (`PHASE10_STATE.json`
`applies_from: "§11"`).

- **Candidate SHA:** `e16e9c78e7f50d8ce4d7442caf4ed61b3e668a6a` —
  `phase10(§11): BLD-04 PASSES — the 50ms gate clears after six rounds`
  (Devan's owner-evidence-run commit; no application source changed in it —
  it carries a tooling script, regenerated captures, and the state update
  routing this turn to review).
- **Prior review-2 candidate:** `b21339e66f1bfa173c0413bf5bda909e54c52d0c` (FAIL, F5/F6)
- **Remediation/evidence commits since review-2:** `b1432d5`, `11978a6`,
  `7f798c8`, `15c0b38`, `e16e9c7`
- **Spec:** `docs/phase10-workflow/specs/section-11.md`
- **Ledger:** `docs/phase10-workflow/acceptance/section-11.json`
- **Result:** **FAIL** — 2 new/reconfirmed bounded findings on carried
  criteria (`TST-03`, `VIS-04`). F5 (`BLD-04`) and F6 (`VIS-11`) from
  review-2 are **CLOSED** this turn with fresh, independent evidence.

## Independent gates

| Gate | Result |
|---|---|
| `npm test` | **PASS** — 106 files, 549 tests, zero failures (independent run) |
| `npm run build` | **PASS** — Next.js 16.2.11, TypeScript clean, 18/18 static pages, `/share` smoke pass (independent run) |
| `npm run phase10:acceptance -- check <ledger> --require implementer` | **PASS** (structurally valid) |
| `measure-long-tasks.mjs` (`BLD-04`) | **PASS** — 0ms across 5 fresh contexts, reproduced on two independent server starts |
| `sample-live-rgb.mjs` (`TST-03`) | **FAIL** — ASML deltaE 79.7–81.6, reproduced on two independent server starts |
| Contact sheet (`npm run phase10:capture -- --section 11`) | **10/10** |

This host launches Chromium without difficulty (consistent with `AGENTS.md`'s
Live Verification section): every measurement below ran against a real
production server built and started independently by this reviewer turn, not
against the owner's or implementer's server process.

## F5 — `BLD-04` — CLOSED, pass

Independently re-measured on two separate server starts (not the same process
as the owner's `20:09` capture): five fresh 1440×900 CPU-2× contexts, **zero**
long-task entries in every context, both times. The Long Tasks API only
reports tasks ≥50ms, and `waitForUniverseReady` requires the canvas visible
plus all eight `[data-scene-ticker]` nodes — a broken probe would have thrown,
not returned zero. This closes the sixth-round carried criterion.

Evidence: `docs/phase10-baseline/section-11/raw-owner-profile-20260729-201852.json`,
`docs/phase10-baseline/section-11/raw-owner-long-tasks-20260729-201852.txt`.

## F6 — `VIS-11` — CLOSED, pass

Fresh capture (`asml-panel-type.png`, regenerated by this turn's own
`npm run phase10:capture` run) shows the Windows line reading
`WEEK ▼ 14.3% · 30D ▼ 12.0% · SINCE BUY ▼ 12.0% (SIMPLE)`. 30D renders.

## New finding: F7 — `TST-03` (carried, high risk) — the trail sampler fails on ASML, not NBIS

`sample-live-rgb.mjs` is unmodified from §10 and self-deriving: it reads
`data-trail-sample-x/y` and `data-weekly-return` live from the DOM each run —
there is no hardcoded coordinate, so running it against the rebuilt view
**is** the required re-derivation. Reproduced identically on two independent
server starts:

```
ASML deltaE 79.7 (run 1), 81.6 (run 2) — sampled #3a255c / #504b83,
hue ~245-263°, vs expected loss-red #b3241d (hue ~3°)
```

The sampled colour is a dark blue-violet, nowhere near the expected ramp —
this is not a "close but over 8" miss, it is the sample point landing on
something other than ASML's trail ribbon entirely. Two smaller issues ride
along in the same run: `IBM` deltaE 8.14 (marginally over the gate) and `CRM`
chroma 0.294 (marginally under the 0.3 hue-lock floor); both are close enough
to be measurement-boundary cases rather than clear defects, but are recorded
for the implementer's attention. ASML's bad sample also breaks the
same-direction magnitude-ordering check for `INTC` and `CBRS`, which is a
downstream consequence of the ASML failure, not three independent defects.

**Not root-caused by this review turn** — that is implementer work — but the
most plausible mechanism is named for triage: `VIS-04`'s arc-shortening
change (36–64° → 26–46°, spec §5.4) moved where the trail ribbon actually
ends on screen without a matching update to the `trailSampleAngle` projection
in `OrreryScene.tsx` (`sampleFraction * tailRadians`), so the sample point now
lands past the now-shorter ribbon. This is a hypothesis for the implementer to
confirm or refute with the actual geometry, not a mandated diagnosis.

**Required change.** Re-derive the sample angle/position so it lands inside
the current (26–46°) trail arc for every holding, for both the DOM-reported
coordinate and the visible pixel. Re-run `sample-live-rgb.mjs` unmodified and
confirm all eight holdings clear ΔE ≤ 8 and the hue-lock/ordering checks. Do
**not** loosen ΔE, widen the arc band back out, or exclude ASML from the
fixture to make the number pass.

Evidence: `docs/phase10-baseline/section-11/raw-rgb-pixel-output.txt`,
`docs/phase10-baseline/section-11/raw-rgb-pixel-output-diagnostic.json` (full
8-holding breakdown, non-aborting diagnostic variant of the same script, same
SHA-256 measurement logic),
`docs/phase10-baseline/section-11/scripts/sample-live-rgb-diagnostic.mjs`,
`docs/phase10-baseline/section-11/pixel-samples/overview-trail-samples.png`.

## New finding: F8 — `VIS-04` (carried, critical risk) — blocked on F7

`VIS-04`'s own ledger entry states it "follows `TST-03`'s remaining ... sample."
Because `TST-03` fails this turn (now on ASML rather than NBIS), `VIS-04`
cannot be independently passed — the trail colour/magnitude encoding it
certifies is exactly what the broken sampler cannot currently locate. The
26–46° arc-band change itself is implemented and not separately disputed;
this finding is inherited from F7, not a second independent defect.

**Required change.** Resolves automatically once F7 is fixed and
`sample-live-rgb.mjs` passes for all eight holdings — re-run and record
`VIS-04`'s own evidence at that point (it is a `visual`-kind criterion and
needs the pixel confirmation, not just the passing script).

## Carried criteria carried forward unchanged (owner decisions, not reopened)

`BHV-11`, `VIS-10`, `VIS-02`, `DEF-02`, `BHV-05` remain `carried_by_owner`
exactly as review-2 recorded them. Nothing in this turn's work touched logo
marks, panel width, or Mission Control typography, so there is no new
evidence to grade them against and the owner's standing decisions are not
reopened by this reviewer.

## Newly-reachable matrix run this turn

34 previously-`not_run` criteria were independently exercised and now carry a
fresh `pass` with retained live/command evidence (full detail in the ledger's
`reviewer` field per criterion): `BHV-10`, `BHV-12`, `BHV-13`, `BHV-14`,
`BHV-16`, `BHV-17`, `BHV-18`, `BHV-19`, `BHV-21`, `BHV-22`, `BHV-30`, `VIS-12`,
`VIS-13`, `VIS-15`, `VIS-17`, `VIS-18`, `ACC-10`, `ACC-11`, `ACC-12`, `TST-10`,
`TST-11`, `TST-12`, `BLD-10`, `BLD-11`, `PRV-11`, `PRV-12`, `PRV-14`, plus
re-confirmation of the already-passing `BHV-15`, `TST-13`, `BLD-12`, `PRV-10`,
`PRV-13`.

Six new retained verifier scripts were added this turn (all under
`docs/phase10-baseline/section-11/scripts/`): `review-3-audit.mjs`,
`review-3-audit-2.mjs` (public `/share`, unauthenticated — naming, window
attachment, panel cuts, error furniture, figure formatting, zoom rubber-band,
descent order, sticky strip, correlation copy), `review-3-sun-ring-pixels.mjs`
(direct pixel measurement for `VIS-18`, since no DOM signal exposes the sun's
screen radius), `review-3-owner-audit.mjs` / `review-3-owner-audit-2.mjs`
(owner-authenticated Mission Control + DRAFT rig — logged in via a throwaway
`OWNER_PASSWORD` this review process was started with, never the real
`.env.local`), and `sample-live-rgb-diagnostic.mjs` (a non-aborting variant of
the retained §10 script that reports every holding instead of stopping at the
first failure — diagnostic only, not the graded verifier).

## Unperformed matrix

These remain `not_run` and are **not implicit passes**. Two categories:

**Attempted this turn but inconclusive** (test-harness limitation, not
evidence of a defect either way): `BHV-34` (URL draft-state encoding was
confirmed present and correctly formatted, but the drag-to-URL and
back/forward-undo behaviour could not be conclusively exercised — the
synthetic pointer drag did not clearly register a weight change, and the
fresh page had no prior history entry for `Back` to return to), `BHV-35`
(the `RESET TO BOOK` button's accessible name did not match this turn's
selector guess and was not located by a corrected one before time ran out).

**Not attempted this turn**: `BHV-20`, `BHV-31`, `BHV-32`, `BHV-33`, `VIS-14`,
`VIS-16`, `VIS-19`, `VIS-20`, `MOB-10`, `MOB-11`, `ACC-13`. `VIS-14` has strong
*structural* evidence from reading `ReturnInstrument.tsx` (three hairlines,
solid baseline, start/end dots, endpoint chip, hover crosshair, conditional
benchmark toggle all present in source and consistent with the captured
screenshots already on file) but was not independently live-pixel-measured
this turn, so it is left `not_run` rather than graded from source alone
(`TST-12`'s own rule). `VIS-16` was visually inspected in the overview capture
and looked inconclusive (rings faintly visible near one planet, not obviously
so near others) but this reviewer did not have a reliable live pixel-sampling
method for ring far-side alpha in the time available — it is left `not_run`
rather than filed as a finding without rigorous measurement.

The next reviewer turn (remediation for F7/F8, then re-review) should run
this remaining list alongside re-confirming F7/F8's fix, prioritizing
`VIS-16`/`VIS-19`/`VIS-20` (visual, would block acceptance per the visual-truth
rule) and `MOB-10`/`MOB-11` (high risk, mobile, untouched this turn).

## Contact sheet

`docs/phase10-baseline/section-11/contact-sheet.md` regenerated this turn at
**10/10** (Devan's `npm run phase10:evidence -- 11` run, reused as this
review's contact sheet since it was captured minutes before this review
turn began, against the same unchanged candidate). No visual criterion this
review graded `pass` rests on a stale or substituted capture.
